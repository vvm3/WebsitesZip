import UnderDevelopment from "@/components/AppElements/underDevelopment";
import { SUPPORT_EMAIL_ADDRESS } from "../../../lib/config";

const Guide = () => {
  return <UnderDevelopment supportEmail={SUPPORT_EMAIL_ADDRESS} />;
};

export default Guide;
